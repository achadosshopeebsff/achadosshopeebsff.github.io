name: Atualizar produtos Shopee

on:
  push:
    paths:
      - "fixed-products.json"
      - "scraper/**"
      - "bot-config.json"
      - ".github/workflows/update-products.yml"
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: shopee-products
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Baixar o repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Atualizar SOMENTE os produtos fixos pela Shopee Affiliate API
        working-directory: ./scraper
        env:
          SHOPEE_APP_ID: ${{ secrets.SHOPEE_APP_ID }}
          SHOPEE_APP_SECRET: ${{ secrets.SHOPEE_APP_SECRET }}
        run: node scrape-all.js

      - name: Publicar catálogo atualizado
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add products.json links.json fixed-products.json bot-config.json scraper/
          git diff --staged --quiet || git commit -m "Atualizar produtos fixos Shopee"
          git push
